<h1>tasting rooms</h1>
<div>for <%= @winery.name %></div>

<table>
  <tr>
    <th>Winery</th>
    <th>Name</th>
    <th>Address</th>
    <th>City</th>
    <th>State</th>
    <th>Zipcode</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tasting_rooms.each do |tasting_room| %>
  <tr>
    <td><%= tasting_room.winery_id %></td>
    <td><%= tasting_room.name %></td>
    <td><%= tasting_room.address %></td>
    <td><%= tasting_room.city %></td>
    <td><%= tasting_room.state %></td>
    <td><%= tasting_room.zipcode %></td>
    <td><%= link_to 'Show', winery_tasting_room(@winery) %></td>
    <td><%= link_to 'Edit', edit_winery_tasting_room_path(@winery, tasting_room) %></td>
    <td><%= link_to 'Destroy', winery_tasting_room(@winery), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Tasting room', new_winery_tasting_room_path(@winery) %>
