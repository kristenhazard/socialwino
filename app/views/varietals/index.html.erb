<h1>Listing varietals</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Grape</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @varietals.each do |varietal| %>
  <tr>
    <td><%= varietal.name %></td>
    <td><%= varietal.grape %></td>
    <td><%= link_to 'Show', varietal %></td>
    <td><%= link_to 'Edit', edit_varietal_path(varietal) %></td>
    <td><%= link_to 'Destroy', varietal, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Varietal', new_varietal_path %>
